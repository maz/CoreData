CoreData is a library for Objective-J/Cappuccino that enables easy RESTful communication with a server.

USAGE:

Put the CoreData.j file in your cappuccino project. To easily create a compatible controller in rails, use the included generator script. Copy generators/coredata_generator to ~/.rails/generators

Ex: scipt/generate coredata_controller person