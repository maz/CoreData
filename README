CoreData is a library for Objective-J/Cappuccino that enables easy RESTful communication with a server.

USAGE:

Put the CoreData.j file in your cappuccino project. To easily create a compatible controller in rails, use the included generator script. To install the generator script for the current user, run `rake generators:install`